import Cards from '@/components/Cards/Cards'
import { models } from '@/data/models'

# Models supported

We currently offer support for OpenAI and Anthropic large language models. We will be working on support for other models in the future, including open source models. Reach out if there is a model we don't currently support that you would like to see.

<Cards children={models} />
