import Cards from '@/components/Cards/Cards'
import { languages } from '@/data/languages'

# Languages supported

`.glass` files are transpiled by the `glassc` compiler to source code in the programming language of your choice.

<Cards children={languages} />
