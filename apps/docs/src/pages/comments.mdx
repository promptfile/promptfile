export const description = 'Glass works with different large language models .'

# Comments

Comments **outside** blocks are ignored by the transpiler and are only used to add notes to your prompt. Comments using ` //` **inside** blocks will be included in the prompt. The best way to include comments in the prompt is through string interpolation `${/*Comment here*/}`.
