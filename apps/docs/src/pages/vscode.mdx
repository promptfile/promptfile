export const description =
  'In this guide, we will look at how to use Glass within Visual Studio Code.'

# Visual Studio Code integration

Glass is designed to work seamlessly within [Visual Studio Code](https://code.visualstudio.com). The [Glass extension](https://marketplace.visualstudio.com/items?itemName=foundation.vscode-glass) can be easily installed from the Visual Studio Code Marketplace. {{ className: 'lead' }} {{className: 'lead'}}

## VSCode settings

<Row>
  <Col>
    <Properties>
      <Property name="glass.defaultChatModel" type="string">
        Default model for chat prompts. Can be one of `gpt-3.5-turbo` (default), `gpt-4`,`gpt-4-0314`,`gpt-3.5-turbo-0301`,`claude-v1`, `claude-v1-100k`, `claude-instant-v1`,`claude-instant-v1-100k`,`claude-v1.3`,`claude-v1.3-100k`,`claude-v1.2`,`claude-v1.0`,`claude-instant-v1.1`,`claude-instant-v1.1-100k`,`claude-instant-v1.0`.



      </Property>
      <Property name="glass.defaultCompletionModel" type="string">
        Default model for completion prompts. Can be one of `text-davinci-003` (default),
          `text-curie-001`,
          `text-babbage-001`,
          `text-ada-001`,
          `text-davinci-002`,
          `text-davinci-001`,
          `davinci-instruct-beta`,
          `davinci`,
          `curie`,
          `babbage`,
          `ada`,
      </Property>
      <Property name="glass.language" type="string">
        The language to transpile to. Can be one of `typescript` (default), `javascript`, or `python`.
      </Property>
      <Property name="glass.openaiKey" type="string">
        Your OpenAI API key. You can create one on your [OpenAI dashboard](https://platform.openai.com/account/api-keys).
      </Property>
      <Property name="glass.outputDirectory" type="string">
        Where to output transpiled code. Defaults to `${workspaceFolder}/src`.
      </Property>
    </Properties>
  </Col>
  <Col sticky>

    ```json {{ 'title': '.vscode/settings.json' }}
    {
      "glass.defaultChatModel": "gpt-3.5-turbo",
      "glass.defaultCompletionModel": "text-davinci-003",
      "glass.language": "typescript",
      "glass.openaiKey": "sk-...",
      "glass.outputDirectory": "${workspaceFolder}/src/generated"
    },
    ```

  </Col>
</Row>

## Commands

The Glass extension provides a number of commands to help you work with Glass files.

### `Glass: Transpile all`

The easiest way to transpile `.glass` files into your language of choice is to use the `Glass: Transpile all` command in VS Code. This command will transpile all `.glass` files in your workspace and write the output to thedirectory of your choice.

### `Glass: Transpile current file`

Similar to the `Transpile all` command, but only transpiles the current file.

### `Glass: Run in playground`

This command will open and run the current file in the Glass playground sidebar.

### `Glass: Open settings`

Shortcut to open the Glass settings in your `settings.json` file.

### `Glass: Open documentation`

Shortcut to open the Glass documentation in your browser.
