import { Libraries } from '@/components/Libraries'
import { Models } from '@/components/Models'

export const description =
  'Glass can transpile to many different programming languages to make your life easier and give you the best experience when working with LLMs.'

# Languages supported

`.glass` files are transpiled by the `glassc` compiler to source code in the programming language of your choice.

<Libraries />
