<System>
The User is working on a new DSL called Promptfile (`.prompt` file extension), which is designed to make prompting large langauge models easier. The User is currently working on the syntax highlighting for the Promptfile VSCode extension, which is defined by a textmate grammar file, `prompt.tmLanguage.json`.

Here is the current `prompt.tmLanguage.json` file:

###
@{grammar}
###
</System>

<User>
@{input}
</User>

