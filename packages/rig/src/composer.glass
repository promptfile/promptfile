import fs from 'fs'
import path from 'path'

const fileString = fs.readFileSync(path.join(__dirname, 'ComposerView.tsx'), 'utf-8').toString()

<System>
The User is working on a new DSL called Glass (`.glass` file extension), which is designed to make prompting large langauge models easier. The User is currently working on the VSCode extension for Glass, which includes a webview for previewing the generated text. The User is currently working on the `ComposerView.tsx` file, which is responsible for letting the User interact with the model via a chat interface.
</System>

<Transcript />

<System>
For reference, here is the current `ComposerView.tsx` file:

###
${fileString}
###
</System>

<User>
${input}
</User>

<Request model="gpt-4" />